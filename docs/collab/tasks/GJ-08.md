# 任务卡 GJ-08：让分层判定各自守得住

- 状态：批次 B 第 1 张（信道 #7 派发） ｜ 实现：Sol ｜ 出卡/评审：Claude Opus 5
- 对应 backlog：[M1 · GJ-08](../../40-delivery/backlog.md)
- 前置任务：批次 A 已于 `0e75a78` 合入主干
- 分支：批次 B 统一分支 `task/GJ-B-m1-remainder`，本卡独立 commit
- **体量预授权**：手写 gross 上限 260 行（预算 200 × 1.3；信道 #10 裁决后由 200 上调，双投影与单调性测试面增加）

## 背景（为什么做）

契约指针：`D-043`、[看板呈现契约 §9](../../20-architecture/dashboard-design.md) 规则 7、`DASH-INV-11`、`EVID-INV-27`、[验收基线 §1](../../40-delivery/acceptance-baseline.md)。

本卡的两条都源自批次 A 验收时的实测，**都是出卡侧（我）的疏漏，不是批次 A 的实现缺陷**。批次 A 严格实现了当时的契约，DoD 逐条成立。

### 一、数据区转义目前没有运行期守卫

GJ-01 把结构门禁的判定范围收窄到产物结构后，数据区不再被任何**运行期**规则检查。验收时我做了变异测试：撤掉「范围收窄」但保留 `=` 转义，`test_reporting.py` 22 个用例**全绿**。穷举探测确认了原因——`=` 与 `<` 都被转义后，结构正则在数据区永远不可能命中，两层是纯冗余，第二层无法被任何输入驱动的测试区分。

这不是坏事，但意味着**转义本身的正确性只由测试夹具保证，没有运行期不变量**。若 `_embedded_json` 的某个 `replace` 将来被删掉，产物会带着可断开 `</script>` 的裸字节发布，只有恰好覆盖到的测试会红。

正确形态是两个**各自可测**的层：结构门禁只判产物结构；数据区由自己的规则判「必须已完全转义」。

### 二、归因散文投影在接缝处漏检

`_personal_attribution_prose` 剔除非散文 token 后**无分隔符**直接拼接，被剔除处不留词边界：

```text
['结果', code'X', 'I led this']  ->  '结果I led this'  ->  \bI\b 不命中
```

这**不是批次 A 引入的回归**（改动前的全串拼接在同样输入下同样不命中），但插入分隔符会严格更保守。

## 目标（做成什么样）

`D-043` 的分层判定在这两处都成立到运行期：每一层有自己的规则、自己的失败信号、以及能单独把它打红的测试。

## 涉及范围

- **允许**：`runtime/src/goodjob/reporting.py`、`runtime/tests/test_reporting.py`、`runtime/src/goodjob/analysis.py`、`runtime/tests/test_analysis.py`
- **禁止**：其余一切文件。特别是 `docs/`、`scanner.py`（属 GJ-03/GJ-05）、`frontend/`（属 GJ-04）
- 零新增依赖

## 接口契约

Architect 给定，不得擅改：

1. `render_dashboard_html` 在拼装产物前，对 `_embedded_json` 的输出增加**渲染期后置断言**：数据区不得含裸 `<`、`>`、`=`、`&`、U+2028、U+2029 及现有可见控制符集合。违反抛 `InvalidInputError`，`code` 不变。
2. 该断言是**独立于结构门禁的一层**：结构门禁只看产物结构，数据断言只看数据区。**不得把两者合并回一次全文匹配**——那正是 `D-043` 禁止的形态。
3. 断言的字符集合与 `_embedded_json` 的转义集合必须来自**同一处定义**，不得各写一份常量。转义集合将来增删时，断言自动跟随。
4. 归因判定改为**双投影并集**（信道 #9 L1 → #10 裁决修订，原「插入换行符分隔」条款作废）：
   - 投影甲＝参与 token 值**无分隔拼接**（即当前实现，保持不变）；
   - 投影乙＝同一批 token 值以**换行符分隔**拼接；
   - 判定时**沿用 `_PERSONAL_PATTERNS` → `_OMITTED_SUBJECT_PERSONAL_PATTERNS` 的既有优先级顺序逐个 pattern 检查，任一投影命中即判定为该归因并返回**。不得先各自判定再合并结果——那会改变优先级语义。
5. 两个投影**只作用于归因判定**；`_resolve_tokens` 返回的完整 statement 拼接保持无分隔符，契约不变。
6. **单调性是本条的硬约束**：改动后的判定必须是改动前的**严格超集**——任何在当前实现下能检出某归因的输入，改动后必须检出**同一个**归因，不得降级为更弱的归因（尤其不得降级为 `personal_assertion`）、也不得变为不检出。这正是 #9 L1 拦下的问题：中文 `_PERSONAL_PATTERNS` 依赖 `我实现` 式字面相邻，任何分隔符都会击穿它，而 `_attribution_covers` 中 `responsible` 覆盖 `personal_assertion` 却不覆盖 `implemented`，降级会让「声明 responsible + 陈述我实现了」从被拒变成通过。
7. 契约 4 只会让检出更宽（例如「单位矩阵 I」这类非第一人称的 `I` 更容易被判为个人归因）。**这是刻意选择的方向**：检出偏严只会要求补个人上下文，漏检则会让个人归因绕过校验。不得为压制这类误报去收窄 `_FIRST_PERSON_PATTERN`，也不得修改 `_PERSONAL_PATTERNS` 或 `_attribution_covers` 的任何取值。

## 实现指引

- 契约 3 的做法之一：把转义映射抽成模块级常量（字符 → 转义序列），`_embedded_json` 用它做替换，断言用它的键做检查。注意 `&` 必须先替换的既有顺序不能破坏。
- 契约 1 的断言是 O(n) 单次扫描，发生在冻结之后、拼装之前；不要放进 `_validate_dashboard_document`（那是结构层）。
- 契约 4 的投影乙用换行符而不是空格：换行符不会出现在正常 token 值里参与构词。
- 契约 4 的形态大致是：`for attribution, pattern in _PERSONAL_PATTERNS: if pattern.search(甲) or pattern.search(乙): return attribution`，两组 pattern 表依次走完。这个写法天然满足契约 6 的单调性——投影甲原样保留在判定里，原有命中不可能丢。
- 为什么不能改成「给中文 pattern 加 `\s*` 容忍分隔符」：那会修改 `_PERSONAL_PATTERNS` 的取值，属契约 7 禁区，且影响面覆盖所有位置而不只是 token 接缝。

## 验收标准（DoD）

- [ ] 门禁全绿：`uv run ruff format --check .`、`uv run ruff check .`、`uv run mypy .`、`uv run pytest -q`
- [ ] 新增测试**直接**覆盖数据断言：构造含裸 `<`/`>`/`=` 的数据串直接调用该断言（或注入未转义数据走渲染路径），必须抛 `InvalidInputError`；不得只靠既有用例间接体现
- [ ] 新增测试：转义集合与断言集合同源——遍历该常量的每个字符，确认 `_embedded_json` 输出中它都不以原字符出现
- [ ] 新增测试：接缝用例 `['结果', code'X', 'I led this']` 形态的 Claim 在无个人上下文时被拒绝（改动前应通过，改动后应拒绝）
- [ ] 新增测试：反向用例 `['详见 ', code'rg -i', ' 的用法']` 仍然 `ready`——不得引入对 `code` 内容的误检
- [ ] **单调性测试（契约 6，本卡最关键的一条）**：对中文强归因的四类各写一个用例，确认改动后检出的归因**与改动前逐字相同**，不降级为 `personal_assertion`——至少覆盖 `我实现了…`→`implemented`、`我负责…`→`responsible`、`我主导了…`→`led`、`我当时学到…`→`personal_learning`，且这四类在跨 `code`/`emphasis` 切分时同样保持
- [ ] **单调性回归用例**：声明 `personal_attribution="responsible"` 配上陈述「我实现了…」，改动前后**都必须被拒绝**（`_attribution_covers("responsible","implemented")` 为 `False`）。这条直接锁住 #9 L1 指出的放宽路径
- [ ] 既有 `test_reporting.py`、`test_analysis.py` 全部通过，批次 A 的用例语义不减弱
- [ ] 覆盖 `IMP-28`、`DASH-03`、`IMP-14`、`IMP-22`

## 边界与禁区

- 不改 `_embedded_json` 的转义**集合内容**（只改它的组织方式，使契约 3 成立）。
- 不改 `_validate_dashboard_document` 的判定范围——批次 A 已定，本卡不回退。
- 不改 `_resolve_tokens` 的返回契约。
- 不收窄 `_FIRST_PERSON_PATTERN`，不改 `_PERSONAL_PATTERNS` 与 `_attribution_covers` 的任何取值（契约 7）。
- 不用「先各自判定再合并结果」实现契约 4——那会改变归因优先级语义。
- 不引入 HTML/DOM 解析器。
- 卡面与实际代码矛盾时停工 L1。

## 交付物

交付报告（信道）：做了什么 / DoD 逐条验证方法 / 体量分组统计 / 自主决策节 / 实施发现 L1-L3 / 存疑点。

**额外要求**：本卡请自带**变异自检**，在交付报告里给出每次的实际输出。理由见[验收基线 §1](../../40-delivery/acceptance-baseline.md)：只在固定夹具下成立的门禁不是门禁。三次，缺一不可：

1. 撤掉契约 1 的数据区断言 → 对应测试必须变红；
2. 从契约 4 的判定中撤掉**投影乙**（只留无分隔投影）→ ASCII 接缝用例必须变红；
3. 从契约 4 的判定中撤掉**投影甲**（只留换行投影）→ **单调性测试必须变红**。第 3 次是本卡的核心自检：它证明你的测试真的能拦住 #9 L1 那条降级路径，而不只是碰巧没触发。
