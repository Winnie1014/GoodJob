# 任务卡 GJ-15：闭合看板机检缺口（DASH-10 / DASH-12）

- 状态：已派发，待领取 ｜ 实现：Sol-Impl ｜ 出卡/评审：Sol
- 对应 backlog：里程碑 M2 · 批次 G · GJ-15
- 前置任务：GJ-13 已通过信道 #43 验收并以 merge `682a66d` 合入本地 `main`
- 分支：`task/GJ-15-dashboard-contract-evidence`，从派卡消息指定的本地 `main` 基线拉出
- 当前基线：`b0c29d3`（出卡侧 parity 修订后的本地 `main`；覆盖 #44 初始坐标）
- **验收强度**：对抗（[protocol §2.2](../protocol.md)）——看板把工作区、JD 与用户上下文产生的不可信 token、标识和深链渲染进 UI；攻击者可尝试注入标记、伪造快照身份或诱导跨版本链接静默落到错误对象
- **体量预授权**：主逻辑与稳定语义钩子预算 260 行，合成夹具、双引擎行为断言和变异自检预算 420 行，合计预算 680 行，手写 gross 上限 884 行（680 × 1.3）；由固定构建命令生成的 `dashboard.js` 单列为强制生成物，不得借生成物夹带手写逻辑

> 本卡有两个验收编号，但只有一个可观察目标：把发布账本中仅有的两个 `missing` 看板语义闭合为可独立判红的机检证据。两项共享同一个冻结 `ReportBundle`、离线 renderer 与跨引擎验证入口，不扩展到任何 `partial` 条目。若任一项实施时需要修改公共数据契约、Python renderer 或新增依赖，按 L1 停工，由 Architect 拆卡或改契约。

## 背景（为什么做）

[发布验收证据账本 §4](../../40-delivery/acceptance-evidence.md#4-dash-证据矩阵)将 `DASH-10` 与 `DASH-12` 判为 `missing`：现有 `verify.mjs` 虽访问 `#/v9/overview`，只检查横向溢出，没有证明跨版本链接明确失败，也没有同时打开同岗位的 `completed` / `partial` 两份快照；现有 Python 双渲染测试也没有逐 Claim 比较 Markdown 与 HTML 的字段。

权威通过条件来自[看板呈现契约 §2](../../20-architecture/dashboard-design.md#2-核心对象与导航语义)、[§11](../../20-architecture/dashboard-design.md#11-跨视图一致性不变量)与[§12](../../20-architecture/dashboard-design.md#12-可判定验收规则)：快照身份必须可区分，跨版本深链不得静默落错对象；同一冻结快照的关键 Claim、Evidence 与限制必须在 Markdown 和 HTML 中保持一致，任一差异都阻止发布判定。

本卡只补适格行为证据及其最小可观察钩子，不改上述产品契约，不更新证据账本状态；账本只由 Architect 在验收收口时根据实际证据更新。

## 目标（做成什么样）

在现有离线文件与 `npm run verify` 入口内，形成两组双引擎、非空且可变异判红的行为证据：一组证明同岗位两份不同状态的冻结快照可可靠辨认，且不支持的契约版本明确失败；另一组证明同一个冻结 `ReportBundle` 生成的 Markdown 与实际浏览器 DOM 在规定语义投影上逐项完全一致，没有 HTML 独有结论。

## 涉及范围

- **允许**：
  - `.agents/skills/goodjob-career-review/runtime/frontend/src/dashboard.ts`
  - `.agents/skills/goodjob-career-review/runtime/frontend/scripts/verify.mjs`
  - `.agents/skills/goodjob-career-review/runtime/src/goodjob/dashboard_assets/dashboard.js`（仅由 `npm run build` 从 `dashboard.ts` 生成）
  - `docs/collab/channel.md` 物理 EOF 追加协作消息（元协议豁免）
- **禁止**：其余一切文件。特别禁止修改 `reporting.py`、任何 Python 测试、`model.ts`、`test.mjs`、CSS、`package.json` / lockfile、数据库/schema/migration、Skill 入口、权威设计文档、验收基线、证据账本、backlog、Makefile、CI/workflow、个人数据目录与真实工作区产物
- **依赖**：零新增第三方依赖；只使用仓库已锁定的 Node 标准库、Playwright、esbuild、TypeScript，以及现有 Python renderer

## 接口契约

### 契约 1：公共数据与产物契约不变

1. `ReportBundle` 仍为 `report-bundle-v1`，Markdown、HTML、manifest、数据库与 CLI 的字段/语义均不变；不得为测试新增持久化字段或测试专用 bundle 字段。
2. HTML 仍是可单独复制和 `file:` 打开的单文件产物，不新增网络请求、外部资源或常驻服务。
3. `render_report_markdown` 与 `render_dashboard_html` 必须接收**同一个已计算 digest 的冻结 bundle**；验证脚本不得分别改写两份输入后再声称同源。

### 契约 2：最小 DOM 语义钩子

为避免把 UI 文案或 class 名当数据解析，允许且仅允许在既有元素上增加以下不可见 `data-*` 属性；属性必须通过 DOM `dataset` / `setAttribute` 安全赋值，不拼接 `innerHTML`：

- `.forensic-strip`：`data-package-status`、`data-preparation-run-id`、`data-bundle-sha256`；值与 bundle 原值逐字相等，显示文本继续遵循现有设计。
- `.claim-item`：`data-claim-id`、`data-facets`；`data-facets` 是按输入顺序的 JSON 字符串数组，不使用逗号拼接。
- 每个实际呈现的 Evidence relation 根元素：`data-evidence-id`、`data-relation`、`data-validity`、`data-commit-state`、`data-supported-facets`；facets 同样是 JSON 字符串数组。
- `.degradation-item`：`data-limitation-id`、`data-kind`、`data-severity`、`data-project-id`；`project_id = null` 时使用空字符串，不造哨兵 ID。
- `.route-error`：`data-error-kind="contract-version-mismatch"` 仅用于不支持的版本；普通未知同版本深链使用既有错误呈现且不得冒充该 kind。

这些钩子是冻结内容的可机读镜像，不是新的公共 schema，也不得隐藏或替代用户可见的文字/图标通道。若现有渲染结构无法在不改公共契约的前提下满足该列表，停工 L1。

### 契约 3：DASH-10 双快照与跨版本深链

1. 合成夹具从同一个基础 bundle 生成两份**独立 HTML 文件**并在同一浏览器 context 的两个 page 中同时打开：
   - 两份的 `role.name`、`role_lens.role_lens_id`、JD 输入与岗位假设完全相同；
   - 一份 `package_status=completed`，另一份 `package_status=partial`；
   - `preparation_run_id` 与重新计算后的 `bundle_sha256` 各不相同。
2. 两页身份条必须分别精确映射各自的 status/run/hash；断言既要证明同岗位，也要证明两份身份不相等，不能只检查“身份条存在”或“文案里有 completed/partial”。
3. 在其中一页粘贴不支持的 `#/v9/overview` 深链后必须同时满足：
   - `main .route-error[data-error-kind="contract-version-mismatch"]` 可见；
   - 可见错误正文明确包含“该链接属于其他契约版本”；
   - `location.hash` 仍保留该跨版本路径，不静默改写为 `v1`；
   - `main` 内没有 `.view`、`.view-title`、`.claim-item` 或来自另一快照的对象内容。
4. 对 `v0`、`v2`、`v9` 以及 URL 编码的非版本 payload 至少各跑一次同类失败关闭断言；不得通过源码 `rg`、常量存在或仅检查溢出来代替浏览器行为。

### 契约 4：DASH-12 同源语义投影

验证脚本必须从同一冻结 bundle 真实生成 `report.zh-CN.md` 与 HTML，再从**Markdown 文本**和**浏览器实际 DOM**分别独立提取规范化投影。禁止从 HTML 的 `#report-data` 读取 bundle JSON后与原 bundle 自比，禁止只比较文件 hash，禁止只检查某段字符串存在。

规范化投影至少包含：

- 每个 Claim：`claim_id` 与有序 `facets`；
- 每条 Claim→Evidence relation：`evidence_id`、`relation`、有序 `supported_facets`，以及所指 Evidence 的 `validity`、`commit_state`；
- 每条限制：两种产物都能观察到的作用域（全局或所属 Project ID）、`kind`、`severity`，以及 message / impact / remediation 的可见 token 文本。Markdown 当前不输出 `limitation_id` 或 `filter_route`，因此它们不是跨产物相等性的锚点；HTML 的 `data-limitation-id` 仍必须非空、唯一，用于发现 DOM 漏项/重复，但不得拿它单独判定 parity。

比较前可按“作用域 + kind + severity + 三段可见文本”的稳定语义键排序，但字段值和列表内容必须逐项精确相等。项目限制的作用域必须从 Markdown 的 Project ID 结构上下文与 HTML 的项目视图上下文分别提取；全局限制不得借 HTML 的隐藏字段补造 Markdown 没有的项目 ID。HTML 不得缺项、增项或替换成 Markdown 没有的结论。夹具必须非空，并同时覆盖：至少 4 个 Claim、`current/stale/missing/plan` 四种 Evidence validity、至少 5 种 commit_state、多个 Claim facets、多个 supported facets，以及至少一条全局限制和一条项目限制；零条目投影不得判绿。

Markdown parser 只解析 renderer 已有的结构化 Claim/Evidence/限制行，并在缺字段、重复 ID、无法解析或数量不符时失败关闭；不得“解析失败就跳过”。DOM 投影只读取用户实际可见的渲染元素及契约 2 的钩子。

### 契约 5：验证输出与 assertion 名

`npm run verify` 的 JSON 结果继续使用 `goodjob-browser-verification-v1`，并在 Chromium、WebKit 各产生以下非空 assertion（可增加细分项，不得改名或只在单引擎运行）：

- `dash10-completed-snapshot-identity`
- `dash10-partial-snapshot-identity`
- `dash10-same-role-distinct-snapshots`
- `dash10-cross-version-deep-link-rejected`
- `dash10-cross-version-no-wrong-object`
- `dash12-claim-evidence-parity`
- `dash12-limitation-parity`
- `dash12-no-html-only-conclusions`

正常运行中任一 assertion 缺席、重复名遮蔽、投影为空或 `ok=false` 都必须使命令非零退出。原有 assertion 继续运行，不得删减或降低。

## 实现指引

- 复用现有 `verify.mjs` 的 synthetic bundle、Python 子进程 renderer、`file:` URL、`record()` 和双引擎循环；不要引入第二套测试 runner。
- 建议将“生成同源 Markdown/HTML”“提取 Markdown 投影”“提取 DOM 投影”“比较投影”拆成小函数，错误详情输出首个不一致的稳定 ID/字段，避免只报 `false`。
- 双快照文件写到已忽略的 `frontend/verify-out/`；验证结束不要求提交，也不得把临时产物纳入 Git。
- `dashboard.ts` 只增加契约 2 的语义钩子与跨版本错误 kind；现有导航、视觉布局、文案、键盘行为和 CSP 不作顺手重构。
- 恶意 ID、facet 或 limitation 文本继续走 DOM API 与 `appendTokens`；不得把 JSON facets 直接拼进 HTML 字符串。
- 绿区自主决策仅限测试辅助函数命名、临时文件名和 projection 内部数据结构；必须在交付报告“自主决策”节披露。任何允许列表外文件、公共字段、依赖或 renderer 改动都是 L1。

## 验收标准（DoD）

- [ ] `cd .agents/skills/goodjob-career-review/runtime/frontend && npm run build && npm run build:check && npm test` 全绿；提交的 `dashboard.js` 与 TypeScript 固定构建结果逐字一致。
- [ ] `npm run verify` 在 Chromium 与 WebKit 全绿，八个契约 assertion 在两引擎各恰好出现一次，且原有 assertion 数量不减少。
- [ ] DASH-10 两页在同一 context 同时存在；身份断言精确核对同岗位、completed/partial、不同 run/hash，任一 status/run/hash 交换或抹平都会判红。
- [ ] DASH-10 对四类不支持版本/编码输入均明确报错、保留原 hash 且 `main` 不呈现错误对象；把版本分支改成静默回落 `v1` 后，指定 assertion 必须判红并使命令非零。
- [ ] DASH-12 的 Markdown/DOM Claim/Evidence 投影都非空且 ID 集合完全相等；限制按契约 4 的可观察语义键逐项相等，DOM limitation ID 另行检查非空、唯一；改变任一侧的一个 validity、commit_state、facet、supported facet 或 limitation 文本后，对应 assertion 必须判红并使命令非零。
- [ ] 变异自检至少覆盖三类：抹平双快照身份、静默接受跨版本深链、制造 Markdown/HTML 单字段差异；每类给出实际非零输出，随后恢复工作树并重跑正常 `npm run verify` 全绿。可用临时工作树编辑或测试内 mutation，不能只解释“理论上会失败”。
- [ ] 新增 `data-*` 钩子用包含引号、`<script>`、`javascript:` 与 Unicode 双向控制字符的值跑过真实浏览器；值保持惰性文本/属性数据，无 DOM 执行、无外部请求、无 console/page error，既有 CSP 阳性对照仍能判红。
- [ ] `make gate-release` 在候选 HEAD 全绿；交付报告给出实际 Python 测试数、文档链接数、两引擎 assertion 数，不沿用卡面或历史数字。
- [ ] `git diff --stat` 仅含允许文件与信道追加；`reporting.py`、公共类型/schema、依赖/lockfile、CSS、权威文档和个人数据零改动。

## 边界与禁区

- 本卡只闭合 `DASH-10`、`DASH-12`；不得顺手补 `DASH-05/06/08/11` 等 `partial` 条目，也不得把它们标为 verified。
- 本卡不更新 `acceptance-evidence.md`。即使测试全绿，也只能由 Architect 验收后按证据逐子句裁定账本状态。
- 不执行 CodeRoute/SliverShield 真实工作区扫描，不安装/更新用户级 Skill，不读取 Owner 个人数据库，不 push、不建远程分支、不发 PR。
- 不修改 Markdown/HTML 的业务文案或 Claim 生成逻辑；发现两种 renderer 当前真实不一致时，保留最小失败证据并按 L1 上报，不在本卡越权改 Python renderer。
- 不把测试钩子做成隐藏 UI、全局可写 API或新的 persisted contract；不使用 `innerHTML`、`eval`、网络或服务端。
- 遇到卡面、bundle 类型、renderer 输出与权威文档矛盾：立即停工，按“现象 / 影响 / 证据 / 建议”发 L1，不自行重解释契约。

## 交付物

信道交付报告须包含：DASH-10 与 DASH-12 分项说明；八个 assertion 的双引擎实际结果；三类变异自检的命令和非零摘要；`make gate-release` 候选 HEAD 与动态计数；体量按手写代码 / 强制生成物 / fixture / 信道分组；自主决策；实施发现 L1-L3（无则写“无”）；未覆盖或存疑点。
